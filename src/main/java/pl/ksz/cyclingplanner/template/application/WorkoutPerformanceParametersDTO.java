package pl.ksz.cyclingplanner.template.application;

public record WorkoutPerformanceParametersDTO(Integer avgHeartRate, Integer avgCadence, Integer avgSpeed,
                                              Integer avgPower,
                                              Integer distance) {

}
